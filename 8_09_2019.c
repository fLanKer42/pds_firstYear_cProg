#include <stdio.h>
int main()
{
  int a[30];
  struct tag
  {
    char roll_no.[10];
    char name[30];
    int marks;
  }a[30];
  printf("emter number of entries
  for(i=1;i<30;i++)
    {
      printf("enter roll number  : ");
      scanf("%s",a[i].roll_no.);
      printf("enter name : ");
      scanf("%s",a[i].name);
      printf("enter marks : ");
      scanf("%d",&a[i].marks);
    }
  
     
  
